extends ./layout/default.pug

block vars
  - var bodyAttributes = {  id : 'admin'}


block content

  ul
    li 
      span unstable_restarts: #{unstable_restarts}
    li 
      span restart_time: #{restart_time}
    li 
      span pm_uptime: #{pm_uptime}
    li 
      span status: #{status}

  .btn-group
    button#reload.btn.btn-info reload
    button#stop.btn.btn-danger stop

  p.log-header Log output
  #log